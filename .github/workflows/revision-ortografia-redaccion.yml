name: Revisión de ortografía y redacción

on:
  pull_request:
    paths:
      - '**/*.md'

jobs:
  languagetool:
    name: LanguageTool
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Analizar ortografía y redacción
        uses: reviewdog/action-languagetool@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          language: es
          level: default
          reporter: github-pr-review
          patterns: |
            **/*.md
